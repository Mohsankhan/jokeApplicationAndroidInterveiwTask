<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView android:id="@+id/textField"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardCornerRadius="24dp"
    app:cardElevation="5dp"
    android:layout_marginHorizontal="5dp"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

        <com.google.android.material.textfield.TextInputLayout
            android:id="@+id/text_input_layout"
            style="@style/text_field_on_boarding"
            android:background="@drawable/text_field_rounded_shape"
            android:hint="Password"
            app:passwordToggleEnabled="true"
            android:textColor="@color/on_boarding_title_color"
            android:textColorHint="@android:color/darker_gray"
            app:boxBackgroundMode="outline">

            <com.google.android.material.textfield.TextInputEditText
                android:id="@+id/edit_text"
                android:inputType="textPassword"
                style="@style/onboardig_edit_field"/>
        </com.google.android.material.textfield.TextInputLayout>
        <!--<EditText
            android:id="@+id/email"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            android:gravity="center|left"
            android:hint="@string/prompt_email"
            android:inputType="textEmailAddress"
            android:padding="12dp"
            android:selectAllOnFocus="true"
            android:textColor="@android:color/white"
            android:textColorHint="@android:color/darker_gray" />-->
    </androidx.cardview.widget.CardView>
